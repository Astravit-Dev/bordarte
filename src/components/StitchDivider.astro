---
interface Props {
  class?: string
  /** Use the lighter copper on the dark navy surface. */
  onDark?: boolean
}

const { class: className, onDark = false } = Astro.props
---

<div class:list={[onDark ? 'stitch-line-dark' : 'stitch-line', 'w-full', className]} role="presentation"></div>
